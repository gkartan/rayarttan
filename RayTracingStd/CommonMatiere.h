#ifndef	_COMMONMATIERE
#define _COMMONMATIERE

typedef enum
{
	TEXTURE_SOLID=0,
	TEXTURE_GRANITE,
	TEXTURE_GRADIENT,
	TEXTURE_SPOTTED,
} ENUM_TYPETEXTURE;


typedef	enum
{
	TYPE_DIFFUSE=0,
	TYPE_AMBIENT,
	TYPE_SPECULAR,
	TYPE_TRANSPARENT
} ENUM_TYPECOLOR;

#endif